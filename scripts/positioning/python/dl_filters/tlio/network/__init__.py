# network — local ResNet1D package for TLIO
# Loaded directly via importlib to avoid external/tlio/src/network/__init__.py
# which has transitive imports that conflict with other packages on sys.path.
